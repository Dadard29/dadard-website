export default class Profile {
    constructor(pk, u, d) {
        this.ProfileKey = pk;
        this.DateCreated = d;
        this.Username = u;
    }

    ProfileKey;
    Username;
    DateCreated;
}